name: Update Marketing Calendar
on:
  push:
    branches: [ main ]
    paths:
      - '.github/datafiles/**'
      - 'generate_calendar.py'
  workflow_dispatch:
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # Force Node.js 24 to fix warnings
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas openpyxl
      - name: Generate Calendar
        run: python generate_calendar.py --data-dir ".github/datafiles" --output index.html
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Auto-update calendar (Polished Version)"
          file_pattern: 'index.html'
